import { Chrome } from '../../shared/Chrome/Chrome';
import { SubmitForm } from './components/SubmitForm';

export const Event = () => {
  return (
    <Chrome>
      <SubmitForm />
    </Chrome>
  );
};
