import { Chrome } from '../../shared/Chrome/Chrome';
import { CreateForm } from './components/CreateForm/CreateForm';

export const Create = () => {
  return (
    <Chrome>
      <CreateForm />
    </Chrome>
  );
};
